# Normalize line endings: store LF in the repo. Shell scripts MUST be LF so they
# run on Linux (the Pi); Windows checkouts can still use CRLF for the rest.
* text=auto
*.sh   text eol=lf
*.go   text eol=lf
Makefile text eol=lf
